<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.curtaincall.user.mapper.UserMapper">

	<!-- 전체 리스트 조회-->
	<select id="selectAllUser" resultType="User">
		SELECT * FROM users
	</select>
	
	<!-- 개별 항목 조회 -->
	<select id="selectUser" parameterType="String" resultType="User">
		SELECT * FROM users
		WHERE username = #{username}
	</select>
	
	<!-- 개별 항목 조회 -->
	<select id="selectUserById" parameterType="int" resultType="User">
		SELECT * FROM users
		WHERE user_id = #{userId}
	</select>
	
	<!--유저의 좋아요/리뷰 개수-->
	<select id="selectCount" parameterType="int" resultType="LikeCountDto">
		SELECT
		  (SELECT COUNT(*) FROM like_musical WHERE user_id = #{userId}) AS likeMusicalCount,
		  (SELECT COUNT(*) FROM like_review  WHERE user_id = #{userId}) AS likeReviewCount,
		  (SELECT COUNT(*) FROM like_actor   WHERE user_id = #{userId}) AS likeActorCount,
		  (SELECT COUNT(*) FROM review       WHERE user_id = #{userId}) AS reviewCount;
	</select>
	
	<!-- 이메일 조회
	<select id="getEmail" parameterType="String" resultType="User">
		SELECT * FROM users
		WHERE email = #{email}
	</select>
	
	휴대폰 조회
	<select id="getPhone" parameterType="String" resultType="User">
		SELECT * FROM users
		WHERE phone = #{phone}
	</select>-->
	
	<!--회원가입-->
	<insert id="signup" parameterType="User">
		INSERT INTO users (username, password, name, email, phone, nickname)
		VALUES (#{username}, #{password}, #{name}, #{email}, #{phone}, #{nickname})
	</insert>
	
	<!--수정-->
	<update id="updateUser" parameterType="User">
		UPDATE users SET 
		password = #{password},
		name = #{name},
		email = #{email},
		phone = #{phone}, 
		nickname = #{nickname}
		WHERE username = #{username}
	</update>
	
	
	<!-- 삭제 -->
	<delete id="deleteUser" parameterType="int">
		DELETE FROM users
		WHERE user_id=#{id}
	</delete>

</mapper>